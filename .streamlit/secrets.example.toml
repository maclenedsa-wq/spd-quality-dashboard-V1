# Copy this to `.streamlit/secrets.toml` for local dashboard settings only.
# The actual secrets file is gitignored.
# Google API authentication for local CLI sync should come from Application
# Default Credentials, for example via your organization's ADC bootstrap flow.

GOOGLE_SHEETS_URL = "https://docs.google.com/spreadsheets/d/your-sheet-id/edit#gid=0"
GOOGLE_SHEETS_WORKSHEET = "Health"
GOOGLE_SHEETS_RANGE = ""
